<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dkadtech.hawaiikai.mapper.KeywordInfoMapper">
    <select id="getKeywordInfoList" resultType="com.dkadtech.hawaiikai.domain.KeywordInfo">
        select
            keyword_info_id,
            keyword,
            place_address,
            place_name,
            process_info_id
        from
            tb_keyword_info
    </select>
</mapper>